export enum WEATHER_CATEGORY_IDS {
  CURRENT_WEATHER = 'current_weather',
  FUTURE_WEATHER = 'future_weather',
}

export enum ADVISORY_PROVIDERS {
  UPCAR = 'upcar',
  OUAT = 'ouat',
  RAJAI = 'rajai',
}

export enum WEATHER_PROVIDERS {
  IMD = 'imd',
  VISUAL_CROSSING = 'visual_crossing',
  OUAT = 'ouat',
  RAJAI = 'rajai',
}
